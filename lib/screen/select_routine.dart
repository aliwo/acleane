import 'package:flutter/material.dart';

class SelectRoutine extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('루틴 입력'),
      ),
      body: Center(
        child: Text('루틴 입력 페이지'),
      ),
    );
  }
}
